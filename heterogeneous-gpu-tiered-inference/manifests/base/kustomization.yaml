apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Shared cluster-wide GPU flavor definitions
  - ../../../common
  # Kueue scheduling (ClusterQueues + LocalQueues)
  - ../kueue
  # RHOAI dashboard (HardwareProfiles)
  - ../rhoai
